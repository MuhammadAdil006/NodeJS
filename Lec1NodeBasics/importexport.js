//in this file we will import some array data from directoru named data and export it to another directory named data

//two ways you can write const object

const {people,age}=require('./directoryName');
console.log(people);
console.log(age);